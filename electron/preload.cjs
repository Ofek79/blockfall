// ============================================================
// electron/preload.cjs — contextBridge (empty for now)
//
// Add any safe Node.js APIs to expose here via contextBridge.
// ============================================================

const { contextBridge } = require("electron");

// Example: expose app version
// contextBridge.exposeInMainWorld("electronAPI", {
//   version: process.versions.electron,
// });
